# Iris through Carl

Verified on Tensor on 7 September 2026 through the graphical panel's existing Unix socket.
No separate Iris interface was added. The request used the same `say` command as Carl chat.

The first real request reached Adrian and Iris. Iris completed her response, but process cleanup
prevented the answer from returning. A cancellable stdout reader and bounded child exit grace
now let completed handoffs return. The panel stream also returns on a final answer envelope.
Dedicated pipe fixtures reproduce both failures without a model or GitHub connection.

After installation and backend reload, the real retry completed through Carl, Adrian and Iris.
Iris ran `carl iris status`. Carl returned the recorded $4.98 of $5 allowance and GitHub issue 45
in the same conversation. No review or issue publication was started by the verification request.
The native window was not visually inspected, so this verifies the live panel transport and
conversation pipeline rather than a screenshot of the rendered chat.

Carl also speculated incorrectly that budget exhaustion explained an earlier killed handoff.
That was not a verified cause. His capability instructions now state that review limits queue
work and do not kill handoffs, and that exit code 137 alone does not establish what ran or why.
The status renderer also keeps published drafts out of its unpublished list and labels cached titles.

The isolated deployment source passed 1199 Rust tests with one ignored test, formatting and Clippy.
The three cleanup checks and capability check passed again after the final edits. Iris's Python
suite passed 67 tests. The initial live failure and model speculation are not counted as successful
verification of those behaviors. The successful retry proves result delivery through the real chain.
