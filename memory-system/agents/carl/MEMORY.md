# Carl Memory

The handbook for running the organisation. Read this before you do that
work. It is not loaded into your prompt, so read it rather than working from a memory of it.

Identity, chain of command and authority are in `CLAUDE.md` and in the compiled organisation.
Nothing here grants anything.

What you have learned goes in `memory/learned.md`, not here. `memory/summary.md` says what you
are carrying right now. This file is the procedure.

Store durable context only. Never whole emails, secrets, codes, credentials or unnecessary
personal data.

## What is in here

Read the rows that match the work in front of you. Reading all of it every time is how a
handbook stops being read at all. If you looked for something and no row covered it, add the
row in the same turn you learn the answer.

| Section | Read it when |
|---|---|
| Delegation | Before you hand any work down. Which department owns what, and why you never reach past a lead. |

## Where everything else is

| Read | When |
|---|---|
| `~/Projects/MEMORY/README.md` and `~/Projects/MEMORY/INDEX.md` | Before anything else, every turn. The index says what is in the shared folder and when to read it. Read the rows that match the work, not the folder. |
| `memory/summary.md` | Every turn, first of your own files. What you are carrying right now. |
| `memory/learned.md` | Before deciding something you may already have decided once. |
| `memory/rules.md` | **Never.** Superseded by `learned.md`, kept only so the migration can be checked. |

## Delegation

- Adrian: code. Mason: Factorio. Olivia: operations and anything with a person on the other
  end. Serena: security. Rowan: research.
- You hand to a lead. You never hand to somebody inside a department, and you never spawn a
  worker yourself. Reaching past Adrian to Nora looks harmless once and removes the reason
  Adrian exists.
- If the right lead is not obvious, pick the one whose department owns the outcome and say why.
