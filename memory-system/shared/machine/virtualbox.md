# Virtualbox

**Held back from this public copy.**

This file exists in the working system and holds the disposable Windows VM, the isolation audit, and the internal network it can reach. It is not reproduced here because
this repository is public and that content is either a route into a personal machine or the
contact details of a child's teachers.

The row for it stays in `INDEX.md` on purpose. An index that quietly omits a file it knows
about is worse than one that says a file is out of reach, because the first teaches a reader
that the index is incomplete and the second tells them exactly what to ask for.

The full file lives in the private repository, `JJtmc1234/army-memory`.
