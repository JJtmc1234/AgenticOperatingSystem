{
  "name": "aos-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Runs AgenticOS routines on a schedule.",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test \"dist/test/*.test.js\"",
    "brief": "node dist/index.js daily-brief"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.220"
  }
}
